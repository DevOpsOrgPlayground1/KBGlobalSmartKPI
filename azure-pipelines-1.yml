# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: ThingWorx

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo open url 
    echo See Start-Process -FilePath "<URL>"
- task: PowerShell@2
  inputs:
    filePath: 'Start-Process -FilePath "https://smartproductionlisdev.corp.knorr-bremse.com/"'
  displayName: 'Run a multi-line script'
